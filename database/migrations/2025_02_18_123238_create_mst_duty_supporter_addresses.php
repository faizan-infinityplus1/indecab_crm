<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('mst_duty_supporter_addresses', function (Blueprint $table) {
            $table->id();
            $table->unsignedBigInteger('admin_id')->nullable();
            $table->foreign('admin_id')->references('id')->on('admins')->onDelete('cascade');
            $table->unsignedBigInteger('duty_supporter_id')->nullable();
            $table->foreign('duty_supporter_id')->references('id')->on('mst_duty_supporters')->onDelete('cascade');
            $table->string('duty_supporter_address_type')->nullable();
            $table->string('duty_supporter_address')->nullable();
            $table->timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('mst_duty_supporter_addresses');
    }
};
